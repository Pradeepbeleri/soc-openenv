[project]
name = "soc-analyst-openenv"
version = "1.0.0"
description = "SOC incident response environment for OpenEnv hackathon"
requires-python = ">=3.10"
dependencies = [
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"

[tool.black]
line-length = 100
